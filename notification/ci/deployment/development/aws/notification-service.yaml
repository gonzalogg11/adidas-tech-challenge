apiVersion: v1
kind: Service
metadata:
  name: notification-service
  annotations:
    alb.ingress.kubernetes.io/target-type: ip
  labels:
    run: notification-service
spec:
  ports:
  - port: 80
    targetPort: 8081
    protocol: TCP
  type: NodePort
  selector:
    run: notification